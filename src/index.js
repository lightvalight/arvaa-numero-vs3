import React from 'react';
import ReactDOM from 'react-dom/client';
import NumberGuessingGame from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <NumberGuessingGame />
  </React.StrictMode>
);
